# Cleaning disk
dd if=/dev/urandom of=/dev/sda bs=4096 status=progress

# Pacman update
sudo pacman -Syu

# Pacman remove only package
sudo pacman -R <package>

# Pacman remove all dependencies
sudo pacman -Rcns

# Pacman remove unnecessary packages
sudo pacman -R $(pacman -Qdtq)

# Unknown public key
gpg --recv-key <public-key>
